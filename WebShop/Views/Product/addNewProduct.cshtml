@model WebShop.Classes.Product

@{
    ViewData["Title"] = "addNewProduct";
}


<div>
    <h4>Product</h4>
    @{
        <div class="product-add">
            <a href="#">
                <div class="product-block">
                    <div class="content">
                        <img src="flashDrive.jpg" />
                        <div calss="product-info">
                            <h3>@Model.Name</h3>
                            <span>Price: @Model.Price$</span>
                        </div>
                    </div>
                </div>
            </a>
        </div>
    }
</div>